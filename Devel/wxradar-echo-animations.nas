var location = "/temp/test/";
var filename = getprop("/sim/fg-home") ~ "/Drawing/wxradar-echo.xml";

var create_echo = func() {

	for (var n = 0; n < 8; n += 1)
	{
		
		for (var type = 

		setprop(location ~ "layers/layer[" ~ n ~ "]/type");

	}    

	io.write_properties(filename, location);

}
